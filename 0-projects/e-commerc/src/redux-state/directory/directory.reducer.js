import { MENU_DATA } from "../../utility/constants";
const INITIAL_STATE = MENU_DATA;

const directoryReducer = (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default directoryReducer;
